import 'package:flutter/material.dart';

void main() {
  runApp(ButtonAndBackGround()),
}

class ButtonAndBackGround extends StatelessWidget {
  const ButtonAndBackGround({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.black,
    );
  }
}